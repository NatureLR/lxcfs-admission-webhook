{{- if and .Values.certManager.enable .Values.metrics.enable }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: {{ include "lxcfs-admission-webhook.name" . }}
        helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    name: {{ include "lxcfs-admission-webhook.resourceName" (dict "suffix" "metrics-certs" "context" $) }}
    namespace: {{ .Release.Namespace }}
spec:
    dnsNames:
        - {{ include "lxcfs-admission-webhook.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" $) }}.{{ include "lxcfs-admission-webhook.namespaceName" $ }}.svc
        - {{ include "lxcfs-admission-webhook.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" $) }}.{{ include "lxcfs-admission-webhook.namespaceName" $ }}.svc.cluster.local
    issuerRef:
        kind: Issuer
        name: {{ include "lxcfs-admission-webhook.resourceName" (dict "suffix" "selfsigned-issuer" "context" $) }}
    secretName: metrics-server-cert
{{- end }}
